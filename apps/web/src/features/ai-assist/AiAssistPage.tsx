/**
 * AIアシスト（プロンプト生成 + AI応答JSONの取り込み）。
 * 暫定版（基盤スタブ）: 本実装はユニット10で置き換える。
 */
export default function AiAssistPage() {
  return (
    <section className="card">
      <h2>AIアシスト</h2>
      <p className="muted">プロンプト生成とJSON取り込みはユニット10で実装されます。</p>
    </section>
  );
}
