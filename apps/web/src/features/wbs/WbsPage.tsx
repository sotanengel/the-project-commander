/**
 * WBSエディタ。
 * 暫定版（基盤スタブ）: ツリー編集の本実装はユニット7で置き換える。
 */
export default function WbsPage() {
  return (
    <section className="card">
      <h2>WBS</h2>
      <p className="muted">WBSエディタはユニット7で実装されます。</p>
    </section>
  );
}
